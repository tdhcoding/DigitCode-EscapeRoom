#pragma once
// Sao chép file này thành secrets.h (cùng thư mục) rồi điền giá trị thật.
// secrets.h đã nằm trong .gitignore — không bị commit lên GitHub.

const char* WIFI_SSID     = "YOUR_WIFI_SSID";
const char* WIFI_PASSWORD = "YOUR_WIFI_PASSWORD";
const char* QT_SERVER_IP  = "192.168.1.100"; // IP máy đang chạy DigitCode Qt app
const uint16_t QT_SERVER_PORT = 8080;
